import { StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  subCategoryTitle: {
    marginTop: 20,
    fontSize: 18,
    fontWeight: 'bold',
    color: '#DA552F',
    textAlign: 'center'
  } 
})

export default styles